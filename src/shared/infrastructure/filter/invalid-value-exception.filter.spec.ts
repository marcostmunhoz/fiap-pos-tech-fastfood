import { InvalidValueExceptionFilter } from './invalid-value-exception.filter';

describe('InvalidValueExceptionFilter', () => {
  it('should be defined', () => {
    expect(new InvalidValueExceptionFilter()).toBeDefined();
  });
});
